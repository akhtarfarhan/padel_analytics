# Padel Analytics

This project performs:
- YOLO-based player/ball detection
- MediaPipe pose tracking
- Shot classification (forehand/backhand/serve)

## Structure

- `data/input/` → raw videos
- `data/output/` → processed videos
- `models/` → YOLO weights
- `src/` → source code
